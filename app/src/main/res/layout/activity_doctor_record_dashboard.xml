<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/green_back_2"
    tools:context=".updateRecord.doctorRecord.doctor_record_dashboard">

    <ImageView
        android:id="@+id/update_record_doctor_add"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginStart="25dp"
        android:layout_marginTop="45dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:onClick="enterDoctorRecordAdd"
        app:srcCompat="@drawable/doctor_record_add" />

    <ImageView
        android:id="@+id/update_record_doctor_view"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginTop="45dp"
        android:layout_marginEnd="25dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:onClick="enterDoctorRecordView"
        app:srcCompat="@drawable/doctor_record_view" />
</androidx.constraintlayout.widget.ConstraintLayout>